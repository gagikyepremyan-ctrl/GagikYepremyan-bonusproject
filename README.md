# Simple terminal countdown timer.

## Usage examples:
  python timer.py 10         # 10 seconds
  python timer.py 2:30       # 2 minutes 30 seconds
  python timer.py 1:00:00    # 1 hour

## Behavior:
- Accepts durations as seconds, MM:SS, or HH:MM:SS
- Prints the remaining time each second, with the first line printed after one second.
- Uses only Python standard library.
